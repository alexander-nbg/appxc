# Copyright 2026 the contributors of APPXC (github.com/alexander-nbg/appxc)
# SPDX-License-Identifier: 0BSD

name: reuse-build

on:
  workflow_call:
    inputs:
      python-version:
        required: false
        type: string
        default: '3.12'
    outputs:
      package_version:
        description: Version reported by the installed built wheel
        value: ${{ jobs.validate.outputs.package_version }}

jobs:
  build:
    name: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0  # needed to get versions right from tag history

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ inputs.python-version }}
          cache: 'pip'

      - name: Install build tooling
        run: python -m pip install --upgrade pip build setuptools-scm

      - name: Determine clean package version
        id: clean-version
        shell: bash
        run: |
          package_version="$(python - <<'PY'
          from setuptools_scm import get_version

          print(get_version(version_scheme="guess-next-dev", local_scheme="no-local-version"))
          PY
          )"
          echo "package_version=$package_version" >> "$GITHUB_OUTPUT"
          echo "Clean package version: $package_version"

      - name: Build package
        env:
          SETUPTOOLS_SCM_PRETEND_VERSION: ${{ steps.clean-version.outputs.package_version }}
        shell: bash
        run: |
          echo "SETUPTOOLS_SCM_PRETEND_VERSION=$SETUPTOOLS_SCM_PRETEND_VERSION"
          python -m build

      - name: Upload built distributions
        uses: actions/upload-artifact@v6
        with:
          name: dist-${{ github.run_id }}
          path: dist/
          if-no-files-found: error
          retention-days: 7

  validate:
    name: validate
    needs: build
    runs-on: ubuntu-latest
    outputs:
      package_version: ${{ steps.package_version.outputs.package_version }}

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ inputs.python-version }}
          cache: 'pip'

      - name: Create venv and install test dependencies
        shell: bash
        run: |
          python -m venv .venv
          .venv/bin/python -m pip install --upgrade pip
          .venv/bin/python -m pip install -r tests/requirements.txt

      - name: Download built distributions
        uses: actions/download-artifact@v8
        with:
          name: dist-${{ github.run_id }}
          path: dist/

      - name: Install wheel package
        shell: bash
        run: |
          .venv/bin/python -m pip install --force-reinstall dist/appxc-*.whl

      - name: Run tests from checkout
        shell: bash
        run: |
          .venv/bin/python -m pytest -rfE tests

      - name: Read installed package version
        id: package_version
        shell: bash
        run: |
          package_version="$(.venv/bin/python -c "from importlib.metadata import version; print(version('appxc'))")"
          echo "package_version=$package_version" >> "$GITHUB_OUTPUT"
